/**
 * Check if a tag name is a void element (cannot have children).
 */
export declare function isVoidElement(tagName: string): boolean;
/**
 * Convert HTML tag name to JSX output.
 * Returns the opening tag string, potentially self-closing.
 */
export declare function formatJsxTag(tagName: string, attributes: string, hasChildren: boolean): string;
//# sourceMappingURL=elements.d.ts.map