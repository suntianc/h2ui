# 05.1-01-SUMMARY: CLI Simplification

**Wave:** 1
**Completed:** 2026-05-22
**Requirements:** CLI-07, CLI-08, CLI-09

## Truths Confirmed
- `h2ui convert file.html --type jsx` outputs .jsx files
- `h2ui convert file.html` (no --llm flag) invokes LLM with mode='auto'
- `h2ui convert file.html --llm off` runs without LLM
- `--llm-provider`, `--llm-model`, `--llm-mode` removed from CLI

## Changes Made

### src/cli/index.ts
- Replaced `--no-typescript` with `--type <type>` accepting `tsx|jsx`
- Replaced `--llm` with `--llm <mode>` accepting `on|off`
- Removed `--llm-provider`, `--llm-model`, `--llm-mode` options
- Updated action callback signature

### src/cli/commands/convert.ts
- Changed options parameter signature to use `type?: string` instead of `typescript?: boolean`
- Updated `mergedConfig.typescript` calculation: `(options.type ?? 'tsx') === 'tsx'`
- Replaced LLM config merge logic to use `options.llm !== 'off'` for default-on behavior
- LLM now defaults to `mode: 'auto'` when enabled

## Verification
- `npm run build` passes
- `h2ui convert --help` shows new `--type` and `--llm` options
- Old flags (`--llm-provider`, `--llm-model`, `--llm-mode`) no longer appear in help

## Success Criteria
- [x] CLI help displays --type tsx|jsx (not --typescript/--no-typescript)
- [x] CLI help displays --llm on|off (not separate enable flag)
- [x] CLI help does NOT show --llm-provider/--llm-model/--llm-mode
- [x] `h2ui convert file.html --type jsx` would set typescript=false internally
- [x] `h2ui convert file.html` (no --llm) enables LLM with config from file or defaults
- [x] `h2ui convert file.html --llm off` disables LLM
- [x] npm run build passes without errors
