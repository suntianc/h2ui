---
phase: 08
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/agent/index.ts
  - src/agent/graph/builder.ts
  - src/agent/graph/state.ts
  - src/agent/graph/nodes/plan.ts
  - src/agent/graph/nodes/execute.ts
  - src/agent/graph/nodes/verify.ts
  - src/agent/graph/nodes/repair.ts
  - src/agent/tools/file.ts
  - src/agent/tools/pipeline.ts
  - src/agent/tools/llm.ts
  - src/agent/tools/verify.ts
  - src/agent/verifier/agent.ts
  - src/agent/history/manager.ts
  - tests/agent/tools.test.ts
  - tests/agent/graph/nodes.test.ts
  - tests/agent/history/manager.test.ts
  - tests/agent/verifier/agent.test.ts
  - tests/agent/cli.integration.test.ts
autonomous: false
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05
  - AGENT-06
  - AGENT-07
  - AGENT-08
  - AGENT-09
  - AGENT-10
user_setup:
  - service: npm
    why: "Install TypeScript dependencies for LangGraph"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Environment or .env file"
    dashboard_config: []

must_haves:
  truths:
    - "User can enable agent mode with --agent flag"
    - "Agent declares plan before execution (PLAN phase visible)"
    - "Agent can use read_file, write_file, run_pipeline, run_llm, verify_output tools"
    - "Agent executes PLAN → EXECUTE → VERIFY → REPAIR loop"
    - "Agent stops after 3 repair attempts and reports confidence score"
    - "Agent tracks failed strategies in .h2ui/agent-history.json"
    - "Agent performs semantic validation to ensure repairs address actual problems (not just silence errors)"
    - "Agent reports confidence score (0-100%) calculated via hybrid formula"
  artifacts:
    - path: "src/agent/index.ts"
      provides: "Agent entry point with run() function"
    - path: "src/agent/graph/builder.ts"
      provides: "StateGraph construction with 4 nodes"
    - path: "src/agent/graph/state.ts"
      provides: "AgentState TypedDict"
    - path: "src/agent/tools/file.ts"
      provides: "read_file, write_file tools with Zod schemas"
    - path: "src/agent/tools/pipeline.ts"
      provides: "run_pipeline tool"
    - path: "src/agent/tools/llm.ts"
      provides: "run_llm tool"
    - path: "src/agent/tools/verify.ts"
      provides: "verify_output tool"
    - path: "src/agent/verifier/agent.ts"
      provides: "Verifier Agent for confidence scoring"
    - path: "src/agent/history/manager.ts"
      provides: "History persistence to .h2ui/agent-history.json"
  key_links:
    - from: "src/cli/commands/convert.ts"
      to: "src/agent/index.ts"
      via: "agent.run() call when --agent flag set"
    - from: "src/agent/graph/builder.ts"
      to: "src/agent/tools/*.ts"
      via: "tool binding to LLM"
    - from: "src/agent/history/manager.ts"
      to: ".h2ui/agent-history.json"
      via: "read/write failed strategies"
---

<objective>
Install LangGraph dependencies, create src/agent/ directory structure, implement the core agent graph with all tools, nodes, state, and create test infrastructure. This plan delivers the foundational infrastructure for the autonomous agent (AGENT-01 through AGENT-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-autonomous-agent/08-CONTEXT.md
@.planning/phases/08-autonomous-agent/08-AI-SPEC.md
@src/llm/providers/anthropic.ts
@src/pipeline/index.ts
@src/types/pipeline.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/pipeline.ts:
```typescript
export interface ConvertOptions {
  out: string;
  typescript: boolean;
  strict: boolean;
  split: boolean;
  cssMode: 'module' | 'scoped' | 'inline' | 'global';
  llm?: LLMConfig;
  framework?: 'react' | 'vue3';
}

export interface PipelineContext {
  html: string;
  filePath: string;
  $?: CheerioAPI;
  code?: string;
  outputPath?: string;
  warnings: string[];
  errors: string[];
  options: ConvertOptions;
}
```

From src/llm/providers/anthropic.ts:
```typescript
export function createAnthropicClient(config: LLMConfig): Anthropic
```

From src/pipeline/index.ts:
```typescript
export class Pipeline {
  addStep(step: PipelineStep): void;
  insertStep(index: number, step: PipelineStep): void;
  removeStep(name: string): void;
  async run(initialCtx: PipelineContext): Promise<PipelineContext>;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Create test infrastructure files (Wave 0)</name>
  <files>
    - tests/agent/tools.test.ts
    - tests/agent/graph/nodes.test.ts
    - tests/agent/history/manager.test.ts
    - tests/agent/verifier/agent.test.ts
    - tests/agent/cli.integration.test.ts
  </files>
  <action>
    Create placeholder test files under tests/agent/ directory structure per RESEARCH.md Wave 0 Gaps:

    tests/agent/
    ├── tools.test.ts              # AGENT-03: test tool definitions with Zod schemas
    ├── graph/
    │   └── nodes.test.ts          # AGENT-02, AGENT-05, AGENT-06: test node behavior
    ├── history/
    │   └── manager.test.ts        # AGENT-07: test history persistence
    ├── verifier/
    │   └── agent.test.ts          # AGENT-09, AGENT-10: test confidence scoring
    └── cli.integration.test.ts    # AGENT-01, AGENT-02: test CLI --agent flag

    Each test file should:
    - Use Vitest (existing project test framework)
    - Have proper imports from the source files
    - Include placeholder test cases with descriptive names matching AGENT requirements
    - Be runnable via: vitest run tests/agent --reporter=verbose

    Example structure for tests/agent/tools.test.ts:
    ```typescript
    import { describe, it, expect } from 'vitest';
    // TODO: Import tool functions once implemented in tasks 3-6
    // import { readFileTool, writeFileTool } from '../../src/agent/tools/file';
    // import { runPipelineTool } from '../../src/agent/tools/pipeline';
    // import { runLLMTool } from '../../src/agent/tools/llm';
    // import { verifyOutputTool } from '../../src/agent/tools/verify';

    describe('AGENT-03: Tools with Zod schemas', () => {
      it.todo('read_file tool accepts valid filePath string');
      it.todo('write_file tool accepts filePath and content');
      it.todo('run_pipeline tool wraps Pipeline correctly');
      it.todo('verify_output tool returns { match, confidence, issues }');
    });
    ```
  </action>
  <verify>
    <automated>find tests/agent -type f -name "*.test.ts" | wc -l && ls tests/agent/</automated>
  </verify>
  <done>5 test files created in tests/agent/ directory with placeholder test structure</done>
</task>

<task type="auto">
  <name>Task 1: Install LangGraph dependencies</name>
  <files>package.json</files>
  <read_first>
    - package.json
  </read_first>
  <action>
    Add @langchain/langgraph and @langchain/core to dependencies in package.json. These are required for the StateGraph-based agent loop per AI-SPEC.md Section 3.

    Add these entries to the "dependencies" section:
    - "@langchain/langgraph": "^0.3.2"
    - "@langchain/core": "^0.3.32"

    After editing package.json, run: npm install
  </action>
  <verify>
    <automated>grep -q "@langchain/langgraph" package.json && grep -q "@langchain/core" package.json && npm list @langchain/langgraph @langchain/core --depth=0 2>/dev/null | head -4</automated>
  </verify>
  <done>npm install succeeds with @langchain/langgraph and @langchain/core in node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Create agent directory structure</name>
  <files>
    - src/agent/index.ts
    - src/agent/graph/builder.ts
    - src/agent/graph/state.ts
    - src/agent/graph/nodes/plan.ts
    - src/agent/graph/nodes/execute.ts
    - src/agent/graph/nodes/verify.ts
    - src/agent/graph/nodes/repair.ts
    - src/agent/tools/file.ts
    - src/agent/tools/pipeline.ts
    - src/agent/tools/llm.ts
    - src/agent/tools/verify.ts
    - src/agent/verifier/agent.ts
    - src/agent/history/manager.ts
  </files>
  <read_first>
    - src/types/pipeline.ts
  </read_first>
  <action>
    Create all directories and placeholder files under src/agent/:

    src/agent/
    ├── index.ts                  # Main entry: run(inputPath, outputPath, options) -> Promise<AgentResult>
    ├── graph/
    │   ├── builder.ts           # StateGraph construction
    │   ├── state.ts             # AgentState interface
    │   └── nodes/
    │       ├── plan.ts          # PLAN node
    │       ├── execute.ts       # EXECUTE node
    │       ├── verify.ts        # VERIFY node
    │       └── repair.ts        # REPAIR node
    ├── tools/
    │   ├── file.ts              # read_file, write_file
    │   ├── pipeline.ts          # run_pipeline
    │   ├── llm.ts               # run_llm
    │   └── verify.ts            # verify_output
    ├── verifier/
    │   └── agent.ts             # Verifier Agent
    └── history/
        └── manager.ts           # History persistence

    Each file should export its primary function/interface and include JSDoc comments. The implementations will be added in subsequent tasks.
  </action>
  <verify>
    <automated>find src/agent -type f -name "*.ts" | wc -l && find src/agent -type d | sort</automated>
  </verify>
  <done>13 TypeScript files created in src/agent/ with proper directory structure</done>
</task>

<task type="auto">
  <name>Task 3: Implement AgentState and tools</name>
  <files>
    - src/agent/graph/state.ts
    - src/agent/tools/file.ts
    - src/agent/tools/pipeline.ts
    - src/agent/tools/llm.ts
    - src/agent/tools/verify.ts
  </files>
  <read_first>
    - src/agent/graph/state.ts (placeholder created in task 2)
    - src/agent/tools/file.ts (placeholder created in task 2)
  </read_first>
  <action>
    Implement AgentState and all 5 tools per AI-SPEC.md Section 4:

    **state.ts** - Define AgentState interface:
    ```typescript
    interface AgentState {
      messages: Array<{ role: "user" | "assistant" | "tool"; content: string }>;
      plan: string | null;
      current_phase: "PLAN" | "EXECUTE" | "VERIFY" | "REPAIR" | "DONE";
      input_path: string | null;
      output_path: string | null;
      repair_attempts: number;
      max_repair_attempts: number;
      verification_result: { match: boolean; confidence: number } | null;
      failed_strategies: string[];
      confidence_score: number | null;
      token_count: number;
    }
    ```

    **tools/file.ts** - Implement read_file and write_file using langchain's tool() decorator with Zod schemas per D-01 and D-02:
    - read_file(filePath: string) returns file contents
    - write_file(filePath: string, content: string) writes file and returns confirmation
    - Use z.object({ filePath: z.string() }) and z.object({ filePath: z.string(), content: z.string() }) schemas

    **tools/pipeline.ts** - Implement run_pipeline tool:
    - run_pipeline(inputPath: string, outputPath: string) runs the Pipeline and returns result
    - Import Pipeline from ../../pipeline/index.js
    - Follow the same pattern as convertCommand in src/cli/commands/convert.ts

    **tools/llm.ts** - Implement run_llm tool:
    - run_llm(prompt: string, context?: object) calls LLM and returns response
    - Use createAnthropicClient from ../../llm/providers/anthropic.js

    **tools/verify.ts** - Implement verify_output tool:
    - verify_output(htmlPath: string, componentPath: string) returns { match: boolean, confidence: number, issues: string[] }
    - This is a placeholder - full verification logic is in Plan 03

    Bind all tools to LLM using model.bind_tools(tools) pattern per AI-SPEC.md Section 4.
  </action>
  <verify>
    <automated>grep -l "export.*AgentState\|interface AgentState" src/agent/graph/state.ts && grep -l "read_file\|write_file" src/agent/tools/file.ts && grep -c "tool(" src/agent/tools/*.ts</automated>
  </verify>
  <done>AgentState interface defined; all 5 tools implemented with Zod schemas and langchain tool() decorator</done>
</task>

<task type="auto">
  <name>Task 4: Implement StateGraph with PLAN → EXECUTE → VERIFY → REPAIR loop</name>
  <files>
    - src/agent/graph/builder.ts
    - src/agent/graph/nodes/plan.ts
    - src/agent/graph/nodes/execute.ts
    - src/agent/graph/nodes/verify.ts
    - src/agent/graph/nodes/repair.ts
  </files>
  <read_first>
    - src/agent/graph/builder.ts (placeholder created in task 2)
    - src/agent/graph/nodes/plan.ts (placeholder created in task 2)
  </read_first>
  <action>
    Implement the StateGraph with 4 nodes and conditional edges per AI-SPEC.md Section 4 and D-03, D-04, D-05, D-06:

    **builder.ts** - Create the StateGraph:
    - Import StateGraph from @langchain/langgraph
    - Define channels for all AgentState fields
    - Add 4 nodes: "plan", "execute", "verify", "repair"
    - Add conditional edges per D-03:
      - PLAN → EXECUTE (always)
      - EXECUTE → VERIFY (always)
      - VERIFY → REPAIR if not match, else END
      - REPAIR → EXECUTE if attempts < 3, else END
    - Compile with MemorySaver checkpointer
    - Export compiled app and runAgent function

    **nodes/plan.ts** - PLAN node:
    - System prompt: "You are an HTML-to-component conversion planner. Analyze the input HTML and declare your conversion plan."
    - Output: concise plan visible to user per D-04 (console.log the plan)
    - Set state.plan and transition to EXECUTE phase
    - Per D-12: track token_count but do not enforce hard limit

    **nodes/execute.ts** - EXECUTE node:
    - Use LLM with bound tools to execute the plan
    - Call run_pipeline tool to convert HTML
    - Collect tool results in messages
    - Transition to VERIFY phase

    **nodes/verify.ts** - VERIFY node:
    - Call verify_output tool
    - Parse result: { match: boolean, confidence: number }
    - Transition to REPAIR if not match, else DONE
    - Per D-07: hybrid verification happens in verifier/agent.ts

    **nodes/repair.ts** - REPAIR node:
    - Per D-10: select from repair strategy pool (simplify structure, rewrite attributes, add comments)
    - Per D-11: skip problematic block as last resort
    - Increment repair_attempts
    - Add strategy to failed_strategies list
    - Transition back to EXECUTE
    - Stop if repair_attempts >= 3 per AGENT-06
  </action>
  <verify>
    <automated>grep -l "StateGraph\|addNode\|addConditionalEdges" src/agent/graph/builder.ts && grep -c "PLAN\|EXECUTE\|VERIFY\|REPAIR" src/agent/graph/nodes/*.ts</automated>
  </verify>
  <done>StateGraph compiles successfully with 4 nodes and conditional edges implementing PLAN→EXECUTE→VERIFY→REPAIR loop</done>
</task>

<task type="auto">
  <name>Task 5: Implement Verifier Agent and History Manager</name>
  <files>
    - src/agent/verifier/agent.ts
    - src/agent/history/manager.ts
  </files>
  <read_first>
    - src/agent/history/manager.ts (placeholder created in task 2)
    - src/agent/verifier/agent.ts (placeholder created in task 2)
  </read_first>
  <action>
    Implement the Verifier Agent and History persistence per AI-SPEC.md Section 4b and D-14, D-15, D-19, D-20:

    **verifier/agent.ts** - Verifier Agent for confidence scoring:
    - Create separate LLM agent (use cheaper model: claude-haiku-4 per AI-SPEC.md Section 4)
    - Per D-07: implement hybrid verification:
      1. First: structural diff (DOM structure, attributes, text content)
      2. Second (if needed): LLM semantic verification
    - Per D-20: confidence = verification_result.weight * 0.7 + (1 - repair_attempts/3) * 0.3
    - Return { match: boolean, confidence: number, issues: string[] }
    - Use zod schema for structured output

    **history/manager.ts** - History persistence per D-14, D-15:
    - File path: .h2ui/agent-history.json
    - Functions:
      - readHistory(): AgentHistory | null
      - writeHistory(history: AgentHistory): void
      - addFailedStrategy(threadId: string, strategy: string): void
      - getFailedStrategies(threadId: string): string[]
    - AgentHistory interface:
      ```typescript
      interface AgentHistory {
        threadId: string;
        failedStrategies: string[];
        tokenCount: number;
        lastUpdated: string;
      }
      ```
    - Per D-16: agent reads history before selecting repair strategy
  </action>
  <verify>
    <automated>grep -l "readHistory\|writeHistory\|addFailedStrategy" src/agent/history/manager.ts && grep -c "confidence\|verify" src/agent/verifier/agent.ts</automated>
  </verify>
  <done>Verifier Agent calculates confidence scores; History Manager persists to .h2ui/agent-history.json</done>
</task>

<task type="auto">
  <name>Task 6: Implement agent entry point</name>
  <files>src/agent/index.ts</files>
  <read_first>
    - src/agent/index.ts (placeholder created in task 2)
    - src/agent/graph/builder.ts (completed in task 4)
  </read_first>
  <action>
    Implement the main agent entry point in src/agent/index.ts:

    Export interface AgentResult {
      outputPath: string;
      confidence: number;
      repairAttempts: number;
      plan: string;
      tokenCount: number;
    }

    Export async function run(
      inputPath: string,
      outputPath: string,
      options: { maxRepairAttempts?: number; tokenBudget?: number }
    ): Promise<AgentResult>

    Per AGENT-08: track token_count but only log warning if exceeded, do not hard-stop per D-12.

    The run function should:
    1. Initialize AgentState with input_path, output_path, max_repair_attempts=3, token_count=0
    2. Create thread_id = conversion-{timestamp}
    3. Compile app with MemorySaver checkpointer
    4. Invoke app with initial state
    5. Return AgentResult with outputPath, confidence, repairAttempts, plan, tokenCount

    Import from:
    - src/agent/graph/builder.ts: runAgent
    - src/agent/history/manager.ts: readHistory, writeHistory
  </action>
  <verify>
    <automated>grep -l "export.*run\|AgentResult" src/agent/index.ts && node -e "import('./src/agent/index.ts').then(m => console.log('exports:', Object.keys(m)))" 2>&1 | head -10</automated>
  </verify>
  <done>src/agent/index.ts exports run() function; TypeScript compiles without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Verify agent compiles and basic structure works</name>
  <files>src/agent/</files>
  <read_first>
    - src/agent/index.ts
    - src/agent/graph/builder.ts
  </read_first>
  <action>
    Run TypeScript compilation and basic import check:

    1. npm run build 2>&1 | head -50
    2. If build succeeds, test basic import:
       node -e "import('./src/agent/index.ts').then(m => console.log('OK:', typeof m.run))"

    Fix any TypeScript errors before proceeding.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|Error" | head -20</automated>
  </verify>
  <done>TypeScript compiles successfully; agent module can be imported</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→agent | User provides HTML file path; agent processes and outputs components |
| agent→filesystem | Tools read/write files in user-specified directories |
| agent→LLM | LLM API calls for planning, execution, and verification |
| verifier→LLM | Verifier Agent uses separate LLM for semantic validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | agent-history.json | mitigate | Validate JSON structure on read; backup before write |
| T-08-02 | Information Disclosure | token tracking | accept | Token counts are internal metrics, no PII |
| T-08-03 | Denial of Service | infinite repair loop | mitigate | Max 3 repair attempts enforced in conditional edge |
| T-08-04 | Elevation of Privilege | write_file tool | mitigate | Path traversal check: file must be under output directory |
</threat_model>

<verification>
- npm run build succeeds with no TypeScript errors
- All 13 agent files compile without errors
- import('./src/agent/index.ts') resolves with run() export
- StateGraph compiles with 4 nodes and conditional edges
- All 5 tools have Zod schemas
- 5 test files created in tests/agent/
</verification>

<success_criteria>
- AGENT-01: --agent flag support exists in CLI (Plan 04 will wire it)
- AGENT-02: PLAN node outputs plan to console
- AGENT-03: All 5 tools defined with Zod schemas
- AGENT-04: EXECUTE → VERIFY edge exists
- AGENT-05: REPAIR node loops back to EXECUTE
- AGENT-06: Max 3 repair attempts enforced via conditional edge
- AGENT-07: History manager reads/writes .h2ui/agent-history.json
- AGENT-08: Token count tracked in state
- AGENT-09: Verifier Agent uses hybrid verification (structural + LLM)
- AGENT-10: Confidence score calculated via hybrid formula
</success_criteria>

<output>
Create `.planning/phases/08-autonomous-agent/08-01-SUMMARY.md` when done
</output>
