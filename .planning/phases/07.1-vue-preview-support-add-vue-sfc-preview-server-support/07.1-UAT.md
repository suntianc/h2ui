---
status: diagnosed
phase: 07.1-vue-preview-support-add-vue-sfc-preview-server-support
source:
  - .planning/phases/07.1-vue-preview-support-add-vue-sfc-preview-server-support/07.1-01-SUMMARY.md
started: 2026-05-24
updated: 2026-05-24
---

## Current Test

[testing complete]

## Tests

### 1. Vue preview with --framework vue3 flag
expected: |
  Running `h2u preview --framework vue3 ./vue-output` starts a Vite dev server
  with @vitejs/plugin-vue, and the preview loads without errors.
result: issue
reported: "Build failed: Rollup failed to resolve import "react" from Dashboard.tsx. Vue mode is active but .tsx files with react imports are present in output."
severity: major

### 2. Framework auto-detection
expected: |
  Running `h2u preview ./vue-output` (without --framework) auto-detects that
  .vue files are present and uses Vue mode automatically.
result: issue
reported: "Build failed same as test 1 - auto-detection correctly shows 'Framework: auto (detected vue)' but fails due to .tsx files with react imports in output directory."
severity: major

### 3. React preview still works
expected: |
  Running `h2u preview --framework react ./react-output` still works as before,
  using WebSocket reload (not Vite HMR).
result: issue
reported: "Build failed - Rollup failed to resolve import 'react/jsx-runtime' from main.tsx. React dependencies are not being resolved properly in preview mode."
severity: major

### 4. Mixed framework warning
expected: |
  If output directory contains both .vue and .tsx/.jsx files, a warning is
  displayed and React is used as default.
result: pass
note: "Warning displayed correctly: 'Warning: Mixed Vue and React files detected. Please use separate output directories.'"

## Summary

total: 4
passed: 1
issues: 3
pending: 0
skipped: 0

## Gaps

- truth: "Vue preview with --framework vue3 flag works without errors"
  status: failed
  reason: "User reported: Build failed - Rollup failed to resolve import 'react' from Dashboard.tsx. Vue mode is active but .tsx files with react imports are present in output."
  severity: major
  test: 1
  root_cause: "syncOutputFiles copies ALL .tsx/.jsx files regardless of framework mode. When Vue mode is active, .tsx files with React imports are still copied but vue() plugin cannot resolve React imports. Additionally, react/react-dom packages are not installed."
  artifacts:
    - path: "src/preview/server.ts"
      issue: "syncOutputFiles (lines 79-94) copies .tsx files unconditionally, even when framework is 'vue'"
  missing:
    - "Modify syncOutputFiles to skip .tsx/.jsx files when framework === 'vue'"
    - "Add react and react-dom as devDependencies in package.json"
  debug_session: ".planning/debug/investigation-vue-preview-rollup-react.md"

- truth: "Framework auto-detection correctly detects Vue and uses Vue mode"
  status: failed
  reason: "User reported: Auto-detection correctly shows 'Framework: auto (detected vue)' but build fails due to .tsx files with react imports in output directory."
  severity: major
  test: 2
  root_cause: "Same as gap 1 - syncOutputFiles copies .tsx files in Vue mode, causing build failure"
  artifacts:
    - path: "src/preview/server.ts"
      issue: "syncOutputFiles copies .tsx files unconditionally"
  missing:
    - "Modify syncOutputFiles to skip .tsx/.jsx files when framework === 'vue'"
    - "Add react and react-dom as devDependencies"
  debug_session: ".planning/debug/investigation-vue-preview-rollup-react.md"

- truth: "React preview works with --framework react flag"
  status: failed
  reason: "User reported: Build failed - Rollup failed to resolve import 'react/jsx-runtime' from main.tsx. React dependencies are not being resolved properly."
  severity: major
  test: 3
  root_cause: "The project does not have react and react-dom installed. When Vite's Rollup bundler tries to resolve imports like 'import React from react' and 'import { createRoot } from react-dom/client' in main.tsx, it fails because the packages cannot be found in node_modules."
  artifacts:
    - path: "package.json"
      issue: "Missing react and react-dom in devDependencies"
  missing:
    - "Add react and react-dom as devDependencies in package.json"
  debug_session: ""
