---
phase: 07.1
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/preview/server.ts
  - package.json
autonomous: true
requirements:
  - gap-07.1-01
  - gap-07.1-02
  - gap-07.1-03
gap_closure: true

must_haves:
  truths:
    - "Vue preview mode skips .tsx/.jsx files during sync"
    - "React dependencies are available for Rollup resolution"
    - "Both Vue and React preview modes work correctly"
  artifacts:
    - path: "src/preview/server.ts"
      provides: "Modified syncOutputFiles with framework-aware filtering"
    - path: "package.json"
      provides: "react and react-dom added as devDependencies"
  key_links:
    - from: "src/preview/server.ts"
      to: "syncOutputFiles"
      via: "framework parameter"
      pattern: "syncOutputFiles.*framework"
---

<objective>
Fix two critical issues in the Vue SFC Preview Server:
1. `syncOutputFiles` copies .tsx/.jsx files in Vue mode, causing Rollup to fail on React imports
2. Missing `react` and `react-dom` packages cause React preview mode to fail
</objective>

<context>
@src/preview/server.ts
@package.json
@.planning/phases/07.1-vue-preview-support-add-vue-sfc-preview-server-support/07.1-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add react and react-dom as devDependencies</name>
  <files>package.json</files>
  <action>
    Add "react" and "react-dom" to devDependencies in package.json with versions compatible with @vitejs/plugin-react@^4.7.0 (use "^18.3.1" for both).
  </action>
  <verify>
    <automated>grep -q '"react": "^18.3.1"' package.json && grep -q '"react-dom": "^18.3.1"' package.json</automated>
  </verify>
  <done>react and react-dom ^18.3.1 appear in devDependencies</done>
</task>

<task type="auto">
  <name>Task 2: Modify syncOutputFiles to skip .tsx/.jsx in Vue mode</name>
  <files>src/preview/server.ts</files>
  <action>
    Modify syncOutputFiles function signature to accept a framework parameter (type Framework). In the file copy condition, add an additional check: when framework === 'vue', skip files ending with '.tsx' or '.jsx'.

    Changes to syncOutputFiles (lines 79-94):
    - Add framework: Framework parameter to function signature
    - Add condition: if (framework === 'vue' && (file.endsWith('.tsx') || file.endsWith('.jsx'))) continue;

    Update call sites:
    - Line 221: syncOutputFiles(outputDir, componentsDir, detectedFramework)
    - Line 263: syncOutputFiles(outputDir, componentsDir, detectedFramework)
  </action>
  <verify>
    <automated>grep -n "syncOutputFiles" src/preview/server.ts | grep -v "^#"</automated>
  </verify>
  <done>syncOutputFiles accepts framework parameter and filters .tsx/.jsx in Vue mode</done>
</task>

</tasks>

<verification>
1. Run `npm install` to install react/react-dom
2. Test React preview: `h2u preview --framework react ./react-output` should work
3. Test Vue preview: `h2u preview --framework vue ./vue-output` should skip .tsx files and work
</verification>

<success_criteria>
- package.json contains react ^18.3.1 and react-dom ^18.3.1 in devDependencies
- syncOutputFiles accepts framework parameter and skips .tsx/.jsx when framework is 'vue'
- Both React and Vue preview modes function correctly
</success_criteria>

<output>
Create `.planning/phases/07.1-vue-preview-support-add-vue-sfc-preview-server-support/07.1-02-SUMMARY.md` when done
</output>
