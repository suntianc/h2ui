---
phase: 07
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - src/pipeline/steps/convert.ts
  - src/pipeline/steps/generate.ts
  - src/engine/css/index.ts
autonomous: true
requirements:
  - VUE-04
  - VUE-06
  - VUE-07
must_haves:
  truths:
    - "Inline styles are extracted to style scoped blocks"
    - "CSS Modules naming convention used (component-name style classes)"
    - "Semantic boundary splitting works with Vue child imports"
    - "Child components are imported with .vue extension"
  artifacts:
    - path: "src/pipeline/steps/generate.ts"
      provides: "Vue child component imports with .vue extension"
      contains: "import .vue"
    - path: "src/pipeline/steps/generate.ts"
      provides: "CSS extracted to style scoped blocks"
      contains: "style scoped"
---

<objective>
Implement CSS extraction to style scoped blocks, semantic boundary splitting with Vue imports, and global.css handling for Vue output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/pipeline/steps/convert.ts
@src/pipeline/steps/generate.ts
@src/engine/css/index.ts
@src/engine/splitter/index.ts
@.planning/phases/07-vue-3-sfc-output/07-CONTEXT.md
</context>

<interfaces>
From src/types/pipeline.ts:
```typescript
interface ComponentNode {
  name: string;
  tag: string;
  element: Element;
  children: ComponentNode[];
  isRepeated: boolean;
  repeatCount?: number;
  cssProperties: Record<string, string>;
}

interface ComponentOutput {
  name: string;
  code: string;
  cssProperties: Record<string, string>;
}

interface CSSFile {
  name: string;
  css: string;
}
```

From src/engine/splitter/index.ts:
```typescript
// splitStep: detects semantic boundaries (header, nav, main, section, footer, etc.)
// Returns SplitResult with root ComponentNode and flatList of all nodes
```

Key decisions from D-04 and RESEARCH.md:
- Per RESEARCH.md Pattern 2 note: "With `<script setup>`, components are auto-registered. The `components: { Child }` pattern in D-04 may be legacy Vue 2 syntax"
- Use Vue 3 idiomatic import-only pattern (no `components: {}` registration)
- Child imports use .vue extension: `import Child from './Child.vue'`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Vue child component import generation</name>
  <files>src/pipeline/steps/generate.ts</files>
  <action>
    Update the Vue SFC generation to properly import child components with .vue extension.

    When generating a parent component that has children (from ctx.componentTree.children):
    - Generate import statements: `import ChildName from './ChildName.vue'`
    - Per Vue 3 `<script setup>` idioms: imported components are auto-registered, no `components: { ChildName }` object needed
    - Place imports in the `<script setup lang="ts">` block before defineProps

    In generateRootComponent for Vue mode:
    - Import child components from their .vue files
    - In template, render child components as `<ChildName />` (self-closing for no children)
  </action>
  <verify>
    <automated>grep -c "\.vue" src/pipeline/steps/generate.ts</automated>
  </verify>
  <done>Vue child components imported with .vue extension in generated SFC</done>
</task>

<task type="auto">
  <name>Task 2: Extract inline styles to style scoped blocks</name>
  <files>src/pipeline/steps/generate.ts, src/engine/css/index.ts</files>
  <action>
    Update generateStep to extract CSS from component's cssProperties and inject into the Vue SFC's `<style scoped>` block instead of writing separate .module.css files.

    Per D-08 and D-09:
    - Use `<style scoped>` with CSS Modules naming convention
    - CSS class names use hyphenated component-style naming: `.my-component { padding: 1rem; }`
    - Global styles (CSS reset, fonts) go to separate global.css file, not in SFC

    In generateVueSFC:
    - Accept cssProperties from ComponentNode
    - Convert cssProperties to CSS string with scoped class name
    - Inject into `<style scoped>` block

    For global.css (D-09):
    - If original HTML has style tags, extract them to global.css
    - Root component imports global.css: `import '../global.css'` or `import './global.css'`
  </action>
  <verify>
    <automated>grep -c "style scoped" src/pipeline/steps/generate.ts</automated>
  </verify>
  <done>Inline styles extracted to style scoped blocks in generated .vue files</done>
</task>

<task type="auto">
  <name>Task 3: Wire component splitting for Vue output</name>
  <files>src/pipeline/steps/convert.ts</files>
  <action>
    Ensure component splitting (splitStep) works correctly with Vue output. The splitStep is framework-agnostic and should not need changes.

    In convertStep for Vue mode:
    - Use renderVueTemplate instead of generateJsxFromNode for template content
    - Pass component tree to generateStep for multi-file output
    - Each component in ctx.components should be a complete .vue SFC

    Verify that:
    - Semantic boundaries (header, nav, main, section, footer, article, aside) trigger component split
    - Each split component becomes its own .vue file
    - Root component imports child components
    - CSS is properly distributed across components (inherited styles not duplicated)
  </action>
  <verify>
    <automated>grep -c "componentTree" src/pipeline/steps/convert.ts</automated>
  </verify>
  <done>Component splitting correctly produces separate .vue files with proper imports</done>
</task>

<task type="auto">
  <name>Task 4: Handle global.css generation for Vue output</name>
  <files>src/pipeline/steps/generate.ts</files>
  <action>
    When Vue output is generated and original HTML has style tags:
    - Extract style tag contents to global.css file
    - Root component imports global.css
    - global.css is written alongside the .vue files in output directory

    Per D-09: "全局样式（CSS reset、字体定义等）抽取到独立的 global.css 文件，不污染组件样式"

    In generateStep for Vue mode:
    - Check for style tags in original HTML ($('style').length > 0)
    - Generate global.css with extracted styles
    - Root component `<script setup>` includes: `import './global.css'`
  </action>
  <verify>
    <automated>grep -c "global.css" src/pipeline/steps/generate.ts</automated>
  </verify>
  <done>Global styles extracted to global.css when original HTML has style tags</done>
</task>

</tasks>

<verification>
After implementation:
1. npm test -- --run tests/vue.test.ts -t "scoped css" passes
2. npm test -- --run tests/vue.test.ts -t "component split" passes
3. npm test -- --run tests/vue.test.ts -t "child imports" passes
</verification>

<success_criteria>
- Child components imported with .vue extension (no components: {} registration)
- CSS extracted to style scoped blocks with proper class names
- Global styles in separate global.css file
- Semantic boundary splitting produces separate .vue files
</success_criteria>

<output>
Create .planning/phases/07-vue-3-sfc-output/07-03-SUMMARY.md when done
</output>
