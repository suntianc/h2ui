---
phase: 07
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/vue.test.ts
  - tests/fixtures/vue/.gitkeep
autonomous: false
requirements:
  - VUE-01
  - VUE-02
  - VUE-03
  - VUE-04
  - VUE-05
  - VUE-06
  - VUE-07
must_haves:
  truths:
    - "Tests can verify Vue SFC output behavior"
    - "Test fixtures exist for Vue conversion scenarios"
  artifacts:
    - path: "tests/vue.test.ts"
      provides: "Vitest test suite for VUE-01..VUE-07"
      min_lines: 50
    - path: "tests/fixtures/vue/index.html"
      provides: "Sample HTML for Vue conversion testing"
      min_lines: 20
---

<objective>
Create Wave 0 test infrastructure and install prettier-plugin-vue with human verification. This establishes the foundation for Vue SFC output development.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/types/pipeline.ts
@src/pipeline/steps/generate.ts
@src/pipeline/steps/convert.ts
@.planning/phases/07-vue-3-sfc-output/07-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Install prettier-plugin-vue with human verification</name>
  <files>package.json</files>
  <action>
    Install `prettier-plugin-vue@1.1.6` via npm. Per RESEARCH.md, this package is [ASSUMED] and requires human verification because slopcheck was unavailable at research time.

    Run: npm install prettier-plugin-vue@1.1.6

    After install, verify the package works by checking:
    1. package.json contains "prettier-plugin-vue" in dependencies
    2. node_modules/prettier-plugin-vue exists

    Note: RESEARCH.md Section "Common Pitfalls > Pitfall 4" warns that prettier-plugin-vue may not handle TypeScript in `<script setup lang="ts">` correctly. Be prepared to format `<script setup>` content separately with TypeScript parser if issues arise.
  </action>
  <verify>
    <automated>grep -q "prettier-plugin-vue" package.json && ls node_modules/prettier-plugin-vue 2>/dev/null && echo "installed"</automated>
  </verify>
  <done>prettier-plugin-vue@1.1.6 installed and verified in node_modules</done>
</task>

<task type="auto">
  <name>Task 1: Create tests/vue.test.ts with VUE-01..VUE-07 stubs</name>
  <files>tests/vue.test.ts</files>
  <action>
    Create tests/vue.test.ts with Vitest test suite covering VUE-01 through VUE-07. Each test should be a stub (describe.skip or test.skip placeholder) that will be implemented during execution.

    Required test cases:
    - VUE-01: "framework vue3" — --framework vue3 flag is recognized
    - VUE-02: "SFC structure" — Generated .vue files contain template, script setup, and style scoped blocks
    - VUE-03: "event binding" — onclick → @click, oninput → @input conversion
    - VUE-04: "scoped css" — style attributes extracted to style scoped blocks
    - VUE-05: "script setup" — script setup lang="ts" present in output
    - VUE-06: "component split" — Semantic boundary splitting works with Vue output
    - VUE-07: "child imports" — Child components imported via import statement

    Read src/pipeline/steps/convert.ts and src/pipeline/steps/generate.ts for patterns to follow.
    Use the same test structure as existing project tests if any exist.
  </action>
  <verify>
    <automated>ls tests/vue.test.ts && grep -c "VUE-0" tests/vue.test.ts</automated>
  </verify>
  <done>tests/vue.test.ts exists with 7 stub tests covering VUE-01..VUE-07</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/fixtures/vue/ with sample HTML</name>
  <files>tests/fixtures/vue/index.html</files>
  <action>
    Create tests/fixtures/vue/index.html with a sample HTML page that exercises Vue SFC conversion scenarios:
    - onclick handler (should become @click)
    - oninput handler (should become @input)
    - disabled boolean attribute (should become :disabled)
    - class attribute (should stay class, not className)
    - for attribute on label (should stay for, not htmlFor)
    - inline style attribute (should be extracted to style scoped)
    - semantic HTML: header, nav, main, section, footer (for component splitting)
    - Nested elements to test child component extraction

    Include at least 50 lines of realistic HTML content.
  </action>
  <verify>
    <automated>ls tests/fixtures/vue/index.html && wc -l tests/fixtures/vue/index.html | awk '{print $1}'</automated>
  </verify>
  <done>tests/fixtures/vue/index.html exists with 50+ lines of Vue-conversion test HTML</done>
</task>

</tasks>

<verification>
After Wave 0: npm test -- --run tests/vue.test.ts should pass (stubs skip, no failures)
</verification>

<success_criteria>
- prettier-plugin-vue@1.1.6 installed and verified
- tests/vue.test.ts exists with 7 stub tests for VUE-01..VUE-07
- tests/fixtures/vue/index.html exists with 50+ lines
</success_criteria>

<output>
Create .planning/phases/07-vue-3-sfc-output/07-01-SUMMARY.md when done
</output>
