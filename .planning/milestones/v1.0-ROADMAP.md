# Milestone v1.0: MVP

**Status:** ✅ SHIPPED 2026-05-22
**Phases:** 1-05.1
**Total Plans:** 16

## Overview

h2ui MVP — CLI tool that converts HTML pages into production-ready React component trees with CSS Modules extraction. Delivered in 1 day with 16 plans across 6 phases.

## Phases

### Phase 1: Core CLI + HTML→JSX/TSX Pipeline

**Goal:** Working CLI that converts basic HTML to valid React TSX/JSX and writes files
**Depends on:** -
**Plans:** 2 plans

- [x] 01-01: Project initialization with TypeScript, Commander, test infra
- [x] 01-02: TypeScript configuration, JSX attribute conversion, SVG handling

**Details:**
Requirements: CLI-01~06, JSX-01~11, CFG-02
Total requirements: 18
Stack: Cheerio, commander, TypeScript, Prettier, vitest

**Success criteria:**
1. User can run `h2ui input.html --out ./components` and get TSX files written to disk
2. `class` → `className`, `style="..."` → `style={{...}}`, `for` → `htmlFor` all work
3. Void elements render as self-closing `<br />`
4. SVG attributes are properly camelCased
5. `--no-typescript` generates `.jsx` instead of `.tsx`
6. Invalid file paths show meaningful error messages
7. Pipeline architecture is in place — steps are independently testable

---

### Phase 2: Component Splitting + CSS Extraction

**Goal:** Split HTML into component tree and extract styles to CSS Modules
**Depends on:** Phase 1 pipeline
**Plans:** 3 plans

- [x] 02-01: Attribute mapping and style parser
- [x] 02-02: Pipeline steps and utilities
- [x] 02-03: Engine + CSS engine integration

**Details:**
Requirements: SPL-01~05, CSS-01~07
Total requirements: 13
Stack: css-tree

**Success criteria:**
1. `<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, `<footer>` each become separate component files
2. Parent components import child components with proper paths
3. Repeated card-like structures are extracted as reusable components
4. Inline `style="..."` is extracted to `.module.css` files
5. Only explicitly-set CSS properties appear in output (no inherited fluff)
6. Shared styles are deduplicated; shorthand properties condensed
7. Component tree is displayed in console after conversion

---

### Phase 3: Configuration + Polish

**Goal:** Config file support and DX improvements
**Depends on:** Phase 1 CLI structure
**Plans:** 4 plans

- [x] 03-01: Cosmiconfig-based config loading with CLI-flag merge priority
- [x] 03-02: Enhanced h2ui init with all 5 config fields scaffold
- [x] 03-03: Ora spinner integration with file count + component tree display
- [x] 03-04: Levenshtein-based error suggestions with 'Did you mean...'

**Details:**
Requirements: CFG-01
Plus polish work: progress spinners, colorized output, component tree preview, better error messages

**Success criteria:**
1. User can create `.h2uirc` with custom defaults
2. CLI flags override config file values
3. Conversion shows nice terminal output with progress
4. Error messages include actionable suggestions

---

### Phase 4: LLM Integration

**Goal:** Optional LLM pass for smarter component naming and cleanup suggestions
**Depends on:** Phase 1+2 output structure
**Plans:** 3 plans

- [x] 04-01: SPL-06 heuristic rules, LLM config types/providers, tiktoken, Zod schemas
- [x] 04-02: LLM review service + pipeline step
- [x] 04-03: CLI integration (--llm flag, config merge, suggestion display)

**Details:**
Requirements: LLM-01~05, SPL-06
Total requirements: 6
Stack: openai, @anthropic-ai/sdk, zod, tiktoken

**Success criteria:**
1. `--llm` flag invokes optional LLM pass
2. Component names are context-appropriate (not just "Header", "Section")
3. Multiple LLM providers work (OpenAI, Anthropic, Ollama)
4. Token estimation shown before API call
5. Tool works identically without LLM — no regression

---

### Phase 5: LLM Code Modification + Browser Preview

**Goal:** LLM directly modifies component code and provides interactive browser preview
**Depends on:** Phase 4 LLM foundation
**Plans:** 2 plans

- [x] 05-01: LLM code modification service
- [x] 05-02: Browser preview server (Vite + WebSocket live reload)

**Details:**
Requirements: LLM-06, LLM-07, POL-01
Total requirements: 3
Stack: vite, ws

**Success criteria:**
1. LLM modifies component code without manual intervention
2. `h2ui preview` starts local preview server
3. Browser shows component tree with interactive editing

---

### Phase 05.1: LLM HTML Fidelity Validation (INSERTED)

**Goal:** Merge LLM review+modify into single fidelity step; simplify CLI flags
**Depends on:** Phase 5
**Plans:** 2 plans

- [x] 05.1-01: CLI simplification (--type, --llm on/off, remove deprecated flags)
- [x] 05.1-02: LLM fidelity step merge (llmFidelityStep replacing review+modify)

**Details:**
Requirements: Fidelity-01, Fidelity-02, CLI-07, CLI-08, CLI-09
D-11 decision: fallback behavior preserved

---

## Phase Dependency Graph

```
Phase 1 (Core CLI + JSX)
    │
    ▼
Phase 2 (Splitting + CSS) ──depends on── Phase 1 pipeline
    │
    ▼
Phase 3 (Config + Polish) ──depends on── Phase 1 CLI structure
    │
    ▼
Phase 4 (LLM) ──depends on── Phase 1+2 output structure
    │
    ▼
Phase 5 (LLM Modify + Preview) ──depends on── Phase 4 LLM foundation
    │
    ▼
Phase 05.1 (LLM Fidelity + CLI Simplification)
```

---

## Key Decisions

- D-06: No LLM caching — each conversion triggers fresh LLM call
- D-10: Graceful degradation when LLM unavailable
- D-11: Fallback behavior preserved in fidelity step
- CLI flags consolidated: `--type tsx|jsx`, `--llm on|off`

## Technical Debt

- Some diagnostic warnings remain in build output (unused imports, etc.)

---

_For current project status, see .planning/ROADMAP.md_
